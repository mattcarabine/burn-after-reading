{
  "name": "burn-after-reading-backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.0.16",
    "wrangler": "^3.0.0"
  }
}